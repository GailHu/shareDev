# 技术方案文档模板

方括号为填写说明，成稿时删除。表格行数按实际增减，宁少勿凑。选型论证与备选比较不写进本文档，那属于技术路线。

---

# <项目名> 技术方案 V0.1

| 项目 | 内容 |
|------|------|
| 对应技术路线 | [技术路线文档名与版本；无路线时填「无，见第 1.2 节决策快筛」] |
| 对应需求 | [需求文档名与版本] |
| 状态 | 草案，待确认第 10 章前提后定稿 |
| 适用范围 | [本方案覆盖哪些模块与场景，明确不覆盖什么] |

**一句话方案**：[用一句话讲清实现思路，要能让读者立刻知道主要工作量落在哪。不是复述需求目标。]

---

## 1. 范围与前置决策

### 1.1 落实的路线决策

| 路线决策 | 本方案的落实位置 | 落实方式 |
|----------|------------------|----------|
| D1 [决策摘要] | 第 X 章 | [具体如何体现] |

### 1.2 决策快筛（仅当无技术路线时填写）

| 决策项 | 结论 | 理由 |
|--------|------|------|
| 数据真值来源与一致性机制 | | |
| 同步与异步的边界、超时与重试 | | |
| 鉴权与访问限制的强制点 | | |
| 抽象与复用边界 | | |
| 技术栈继承还是新增 | | |

### 1.3 路线冲突项

[某条路线决策在实现层面走不通时填写，交用户裁决后再继续。无冲突写「无」。]

| 冲突的决策 | 走不通的具体原因 | 建议替代做法 |
|------------|------------------|--------------|

---

## 2. 事实基线

以代码仓库为准，与需求文档描述不一致的差异记入第 10 章。

| 领域 | 现状 | 本方案的处理 |
|------|------|--------------|
| 数据模型 | | [沿用 / 改造 / 新增] |
| 接口风格与命名 | | |
| 错误码体系 | | |
| 鉴权机制 | | |
| 日志与埋点规范 | | |

**新增项及理由**：[每个标 [新增] 的元素写一句为什么不能沿用。]

---

## 3. 数据模型

### 3.1 表结构

**表名**：`[table_name]`　**标注**：[沿用 / 改造 / 新增]　**用途**：[一句话]

| 字段 | 类型 | 可空 | 默认值 | 约束与说明 |
|------|------|------|--------|------------|
| | | | | |

**索引**：[索引名、字段组合、用于支撑哪条查询]

**其他**：[软删策略、时区约定、小数精度、预估数据量与增长速度]

### 3.2 实体关系

```mermaid
erDiagram
```

---

## 4. 接口契约

**契约章节的验收标准：写完后前端、测试、下游服务可脱离后端并行开工。**

### 4.1 [接口名]

| 项目 | 内容 |
|------|------|
| 路径与方法 | `POST /api/v1/xxx` |
| 标注 | [沿用 / 改造 / 新增] |
| 鉴权 | [所需身份与权限] |
| 幂等键 | [字段名与生成规则；不涉及写操作填「不适用」] |

**请求体**

```ts
// 字段写全，不写「其他字段略」
```

**响应体**

```ts
```

**错误码**

| 错误码 | HTTP 状态 | 触发条件 | 用户可见文案 |
|--------|-----------|----------|--------------|

### 4.2 共享类型定义

```ts
// 前后端或服务间共用的结构，就地给出定义
```

---

## 5. 枚举与状态机

### 5.1 枚举

| 枚举 | 取值 | 含义 | 可扩展性 |
|------|------|------|----------|

### 5.2 状态机

```mermaid
stateDiagram-v2
```

**迁移表**

| 起始状态 | 目标状态 | 触发动作 | 前置校验 | 副作用 |
|----------|----------|----------|----------|--------|

**非法迁移的处置**：[返回哪个错误码、是否落日志、是否告警。这一行不能省。]

---

## 6. 关键链路

### 6.1 [链路名]

```mermaid
sequenceDiagram
```

| 项目 | 内容 |
|------|------|
| 事务边界 | [落在哪几步之间] |
| 不可重入步骤 | [哪一步、为什么] |
| 超时与重试 | [每个外部调用的超时值、重试次数、总耗时上限] |
| 幂等机制 | [重试依赖的幂等键。有重试无幂等即不合格] |
| 失败回收 | [失败在第 N 步时，已产生的副作用如何回收] |

---

## 7. 异常与边界

按 checklist.md 扫描清单逐类填写。本章篇幅通常应超过第 6 章。

| 编号 | 类别 | 触发条件 | 系统行为 | 用户可见表现 |
|------|------|----------|----------|--------------|
| E1 | [输入/权限/并发/依赖/数据/恢复] | | | |

**不适用的类别**：[列出并各给一句理由。]

---

## 8. 非功能落地

### 8.1 性能预算

| 链路 | 端到端上限 | 分跳预算 | 余量 |
|------|------------|----------|------|

**最慢的一跳**：[哪一跳，超出预算时如何应对]

### 8.2 可观测性

第 7 章每条异常都要能在此找到对应信号。

| 对应异常 | 信号类型 | 具体定义 | 告警阈值 |
|----------|----------|----------|----------|
| E1 | [日志/指标/追踪] | [日志字段名或指标名] | |

### 8.3 安全

| 项目 | 措施 |
|------|------|
| 访问限制的服务端强制点 | |
| 敏感字段脱敏与落盘范围 | |
| 越权验证用例 | |

### 8.4 兼容与迁移

| 项目 | 内容 |
|------|------|
| 接口兼容性 | [兼容 / 不兼容 + 并存期与下线时间] |
| 数据迁移 | [脚本、校验方式、可重复执行保证] |
| 灰度方案 | [范围与放量节奏] |
| 回滚路径 | [含新格式数据的处理方式，不能只写「回退版本」] |

---

## 9. 任务拆分与验收

按契约先行排序。验收标准须能被第三方判定。

| 编号 | 任务 | 产出物 | 依赖 | 验收标准 | 预估 |
|------|------|--------|------|----------|------|
| T1 | | | — | | |

---

## 10. 待确认前提

以下各项影响具体实现，但不改变本方案的整体结构。确认后本文档升版为 V1.0。

| 编号 | 待确认 | 影响 | 未确认时的默认假设 |
|------|--------|------|--------------------|
| Q1 | | | |

---

## 11. 交付自检

- **能否照着写代码**：[抽一个接口，说明其字段完整到可直接定义类型]
- **能否照着写测试用例**：[抽一条业务规则，说明其边界值与非法输入的预期行为已给出]
- **线上出问题能否定位**：[抽第 7 章一条异常，指出第 8.2 节对应的信号]
